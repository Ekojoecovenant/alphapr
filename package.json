{
	"name": "alphapr",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"tail": "wrangler tail",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest run",
		"lint": "biome check .",
		"format": "biome format --write",
		"typecheck": "tsc --noEmit",
		"cf-typegen": "wrangler types",
		"ship": "pnpm run deploy && pnpm run tail"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.7",
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"@types/node": "^26.1.0",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.107.0"
	}
}
